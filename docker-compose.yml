services:
  web:
    build:
      context: .
    working_dir: /app
    ports:
      - "5173:5173"
      - "4173:4173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
